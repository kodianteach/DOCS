# Historia #10: Configuración de Branding, Theming Dinámico y Navegación Unificada por Rol

**Estado:** En Desarrollo (Developer)

---

## Historia de Usuario

**Como** ADMIN_ATLAS (administrador de la organización),  
**Quiero** configurar el branding visual de mi organización (logo e ícono, colores dominante, secundario y de acento), que el sistema aplique automáticamente estos estilos a todos los usuarios de mi organización al iniciar sesión, y contar con una barra de navegación inferior unificada que se adapte dinámicamente según el rol del usuario,  
**Para** que la plataforma refleje la identidad visual de mi ciudadela, conjunto o condominio, ofrezca una experiencia de navegación consistente y personalizada por rol, y transmita profesionalismo y pertenencia a cada usuario que ingrese al sistema.

## Descripción

Esta historia abarca tres funcionalidades integradas en el módulo de configuración de la organización:

1. **Configuración de Branding:** En el formulario existente de configuración de organización (`/admin/organization-config`), se agregan campos para cargar el logo/ícono de la organización y definir los colores del tema visual. El sistema extrae automáticamente los colores dominantes de la imagen cargada mediante procesamiento en el cliente (TypeScript), autocompletando los campos de color dominante, secundario y de acento. El administrador puede modificar estos colores manualmente si lo desea.

2. **Theming Dinámico por Organización:** Cuando cualquier usuario vinculado a la organización inicia sesión, el sistema valida a qué organización pertenece, obtiene la configuración de branding y aplica los estilos (colores, logo) a toda la plataforma siguiendo la regla 60/30/10. Si el administrador no ha configurado los colores, el sistema usa los colores por defecto actuales. El cambio de estilos se refleja en el próximo login de cada usuario. El sistema genera automáticamente opacidades y variaciones de los colores para fondos, hovers y estados. Todos los componentes del frontend (átomos, moléculas, organismos, templates) deben adaptarse para consumir estos colores dinámicos.

3. **Bottom Navigation Unificado:** Se reemplazan los tres componentes de navegación inferior existentes (`BottomNavComponent`, `AdminBottomNavComponent`, `DoormanBottomNavComponent`) por un único componente reutilizable que se adapta según el rol del usuario. La configuración de ítems del menú se define mediante un JSON en el código TypeScript (no desde base de datos), permitiendo modificar opciones por deploy. El componente se adapta a los estilos de theming de la organización.

---

## Criterios de Aceptación

### Escenario 1: Flujo Principal - Admin configura branding completo

- **Dado** que el ADMIN_ATLAS está autenticado y navega al formulario de configuración de organización (`/admin/organization-config`)
- **Cuando** carga una imagen de logo en formato PNG o JPG válido, el sistema extrae los colores automáticamente, el admin revisa los colores sugeridos y guarda la configuración
- **Entonces** el sistema persiste en base de datos la imagen del logo (en bytes), el color dominante, el color secundario y el color de acento asociados a la organización, y muestra una notificación de éxito

### Escenario 2: Extracción automática de colores desde imagen

- **Dado** que el ADMIN_ATLAS selecciona una imagen válida (PNG o JPG) para cargar como logo de la organización
- **Cuando** la imagen se carga exitosamente en el formulario
- **Entonces** el sistema muestra una previsualización de la imagen cargada, ejecuta un análisis de colores en el cliente (TypeScript puro) extrayendo el color dominante, secundario y de acento, y autocompleta los campos correspondientes en la zona inferior del formulario con los colores identificados

### Escenario 3: Modificación manual de colores autocompletados

- **Dado** que el sistema ha autocompletado los campos de color dominante, secundario y de acento basándose en la imagen cargada
- **Cuando** el ADMIN_ATLAS desea usar colores diferentes a los sugeridos
- **Entonces** el admin puede modificar manualmente cualquiera de los tres campos de color (dominante, secundario, acento) ingresando el valor que prefiera, y los nuevos valores se persisten al guardar el formulario

### Escenario 4: Validación estricta de formato y tamaño de imagen

- **Dado** que el ADMIN_ATLAS intenta cargar una imagen como logo de la organización
- **Cuando** selecciona un archivo que no es PNG ni JPG, o el archivo excede el tamaño máximo permitido (configurable desde `application.yml` del backend)
- **Entonces** el sistema rechaza la carga, muestra un mensaje de error claro indicando los formatos aceptados (PNG, JPG) y el tamaño máximo permitido, y no procesa la imagen

### Escenario 5: Previsualización de imagen cargada

- **Dado** que el ADMIN_ATLAS ha seleccionado una imagen válida para cargar
- **Cuando** la imagen se procesa en el formulario
- **Entonces** se muestra una previsualización visual de la imagen en el formulario antes de guardar, permitiendo al admin confirmar que es la imagen correcta

### Escenario 6: Aplicación de theming al iniciar sesión

- **Dado** que el ADMIN_ATLAS ha configurado y guardado el branding de la organización (logo + colores)
- **Cuando** cualquier usuario vinculado a esa organización (ADMIN_ATLAS, OWNER, RESIDENT, PORTERO_VIGILANTE) inicia sesión en su próximo login
- **Entonces** el sistema valida a qué organización pertenece el usuario, obtiene la configuración de branding, y aplica los colores dinámicamente a toda la plataforma siguiendo la regla 60/30/10: color dominante al 60% (fondos, superficies grandes, contenedores principales), color secundario al 30% (barras, tarjetas, secciones, navegación), color de acento al 10% (botones CTA, links importantes, indicadores, badges)

### Escenario 7: Generación automática de variaciones de color

- **Dado** que el sistema aplica los colores de la organización al cargar la plataforma
- **Cuando** se procesan los colores dominante, secundario y de acento
- **Entonces** el sistema genera automáticamente opacidades y variaciones de cada color para: fondos claros (opacidad baja del dominante), estados hover (variaciones más oscuras/claras), bordes (variaciones sutiles del secundario), y sombras/highlights (derivados del acento), asegurando armonía visual sin intervención manual

### Escenario 8: Colores por defecto cuando no hay configuración

- **Dado** que el ADMIN_ATLAS no ha configurado los colores de branding de su organización
- **Cuando** cualquier usuario de esa organización inicia sesión
- **Entonces** el sistema aplica los colores por defecto actuales del sistema (naranja coral `#FF8C61` como primario y los tokens CSS existentes en `styles.css`) sin afectar la experiencia visual

### Escenario 9: Navegación dinámica unificada - Rol OWNER/RESIDENT (no-admin)

- **Dado** que un usuario con rol OWNER o RESIDENT inicia sesión exitosamente
- **Cuando** se renderiza la barra de navegación inferior
- **Entonces** se muestra un bottom navigation estilo píldora con bordes redondeados, fondo gris claro, con los ítems: Inicio (ícono casa), Historial (ícono reloj), botón central FAB circular flotante "+" en color de acento de la organización (para generación de autorizaciones QR - HU #6), Difusión (ícono megáfono) y Vehículos (ícono automóvil). Los colores del estado activo e inactivo se adaptan a la configuración de theming de la organización

### Escenario 10: Navegación dinámica unificada - Rol ADMIN_ATLAS

- **Dado** que un usuario con rol ADMIN_ATLAS inicia sesión exitosamente
- **Cuando** se renderiza la barra de navegación inferior
- **Entonces** se muestra un bottom navigation de ancho completo, estilo flat/corporativo sin bordes redondeados ni sombras, con indicador superior activo (línea horizontal en color de acento sobre el ítem seleccionado), con los ítems: Inicio (ícono casa), Unidades (ícono edificio), Porteros (ícono usuario/ID), Config (ícono engranaje) y Más (ícono tres puntos). Los colores del estado activo, inactivo e indicador se adaptan al theming de la organización

### Escenario 11: Navegación dinámica unificada - Rol PORTERO_VIGILANTE

- **Dado** que un usuario con rol PORTERO_VIGILANTE inicia sesión exitosamente
- **Cuando** se renderiza la barra de navegación inferior
- **Entonces** se muestra la barra de navegación inferior con los ítems configurados para el rol de portero (definidos en el JSON de configuración del componente), con los colores adaptados al theming de la organización

### Escenario 12: Configuración de ítems de navegación por JSON

- **Dado** que se necesita modificar los ítems del menú de navegación para cualquier rol
- **Cuando** un desarrollador modifica el JSON de configuración en el código TypeScript del componente unificado de navegación
- **Entonces** al hacer deploy, la navegación refleja los nuevos ítems configurados para cada rol sin necesidad de cambios en base de datos ni en múltiples componentes

### Escenario 13: Adaptación de componentes del frontend al theming

- **Dado** que la organización tiene configuración de branding activa
- **Cuando** el usuario navega por las diferentes secciones de la plataforma
- **Entonces** todos los componentes visuales (átomos, moléculas, organismos, templates) del frontend consumen los colores dinámicos de la organización a través de variables CSS, manteniendo consistencia visual en toda la plataforma

---

## Información Recopilada

### Usuario y Contexto

- **Tipo de usuario:** ADMIN_ATLAS (administrador de la organización) para configuración; todos los roles (ADMIN_ATLAS, OWNER, RESIDENT, PORTERO_VIGILANTE) como consumidores del theming y navegación
- **Permisos requeridos:** Acceso al formulario de configuración de organización (permisos de ADMIN_ATLAS)
- **Valor de negocio:** Permite que cada organización (ciudadela, conjunto, condominio) personalice la identidad visual de la plataforma, generando sentido de pertenencia y profesionalismo. Unifica la navegación en un solo componente mantenible y adaptable por rol

### Reglas de Negocio

- Solo se aceptan imágenes en formato **PNG y JPG** para el logo; cualquier otro formato debe rechazarse estrictamente
- El tamaño máximo de imagen es **configurable desde `application.yml`** del backend; la validación se realiza en el backend al recibir la imagen
- La imagen se almacena en el backend en **bytes** (campo binario en base de datos)
- La extracción de colores se ejecuta **completamente en el cliente** (TypeScript puro en el browser), sin procesamiento backend
- Los colores se manejan en formato **hex** (#RRGGBB)
- La regla de distribución de colores es **60/30/10**: dominante (fondos/superficies), secundario (estructura/barras), acento (CTAs/botones)
- Los cambios de branding se aplican en el **próximo login** de cada usuario, no inmediatamente para usuarios ya logueados
- Si no hay configuración de branding, se usan los **colores por defecto** actuales del sistema (naranja coral `#FF8C61` y tokens CSS de `styles.css`)
- El sistema **genera automáticamente** opacidades y variaciones de los colores configurados para fondos, hovers, bordes y estados
- No se requiere recorte ni redimensionamiento de la imagen antes de guardar
- Los ítems de la barra de navegación se configuran mediante un **JSON en el código TypeScript** (no dinámico desde BD), modificable por deploy
- El FAB central "+" en la navegación de no-admin se vincula a la **generación de autorizaciones QR** (funcionalidad de HU #6)
- Los tres bottom-nav existentes se reemplazan por un **único componente reutilizable** que se adapta por rol

### Interfaz

- Los campos de branding se agregan al **formulario existente** de configuración de organización en `/admin/organization-config`
- Al cargar la imagen se muestra una **previsualización** de la imagen subida
- En la zona inferior del formulario se muestran los campos de: **color dominante**, **color secundario** y **color de acento**, autocompletados por el análisis de la imagen
- Los campos de color deben permitir **edición manual** (color picker o input hex)
- **Navegación no-admin (OWNER/RESIDENT):** Bottom nav estilo píldora, bordes redondeados, fondo gris claro (`#EDEAE7` aprox), sombra suave, 90% ancho de pantalla, margen inferior 15-25px. Distribución: [Inicio] [Historial] [+FAB] [Difusión] [Vehículos]. FAB circular 56-64px diámetro, color de acento de la organización, ícono "+" blanco. Íconos outline 22-26px, texto 12-13px. Estado activo: color de acento. Estado inactivo: gris medio `#9E9E9E`
- **Navegación ADMIN_ATLAS:** Bottom nav ancho completo, estilo flat, sin bordes redondeados ni sombras, fondo gris claro sólido (`#E5E5E5`), 70-75px altura. Indicador activo superior: línea horizontal 40-50px × 3-4px en color de acento. Distribución: [Inicio] [Unidades] [Porteros] [Config] [Más]. Íconos outline 24-26px, texto 12px. Sin botón FAB flotante
- **Navegación PORTERO_VIGILANTE:** Configurable mediante JSON en TypeScript, adaptado a theming
- Tipografía sans-serif moderna (Inter, SF Pro, Poppins). Peso activo: 500 (Medium), inactivo: 400 (Regular)
- Diseño minimalista, sin bordes visibles ni líneas divisorias

### Sistemas Externos

Ningún sistema externo involucrado. Todo el procesamiento de colores se realiza en el cliente.

---

## Contexto y Referencias

**Arquitectura:** La organización ya tiene campo `settings JSONB` en la entidad `Organization` del backend. Existe `OrganizationConfiguration` como tabla extendida. En el frontend existe `OrganizationConfigGateway` y `OrganizationConfigAdapter`. Los CSS Design Tokens están definidos en `styles.css` con variables CSS para colores, spacing, tipografía y soporte dark mode.

**Historias relacionadas:** HU #1 (Login Multitenant - JWT con `organizationId`, roles, módulos), HU #2 (Onboarding Admin - creación de Organization con tipos CIUDADELA, CONJUNTO, CONDOMINIO), HU #6 (Generación de Autorizaciones QR - vinculada al FAB central), HU #8 (Panel Administrativo - propone RoleGuard y directiva `*appHasRole`), HU #9 (Invitaciones - extiende OrganizationConfiguration, propone `owner-bottom-nav`)

**Lecciones aprendidas:** 
- Los colores actuales son globales y hardcodeados, no dinámicos por organización; se necesita un servicio de theming
- Los 3 bottom-nav existentes tienen lógica hardcodeada con `@if (isAdmin())` en templates; unificarlos en un solo componente mejora mantenibilidad
- El `TenantNavigationService` ya existe para navegación por módulos; puede ser base o referencia para la navegación por rol
- El JWT ya contiene `organizationId` lista para usarse al obtener la configuración de branding en el login

---

<!-- ============================================================================ -->
<!-- PUNTO DE INSERCIÓN: Análisis Arquitectónico (Arquitecto)                    -->
<!-- ============================================================================ -->
<!-- Agregado por: Workflow analizar-disenar-historia-usuario                    -->
<!-- Template: seccion-analisis-arquitectonico.template.md                       -->
<!-- Descripción: El Arquitecto analiza la HU y agrega decisiones de diseño,     -->
<!--              patrones arquitectónicos, componentes afectados y referencias   -->
<!-- Estado después de inserción: "Analizado (Arquitecto)"                       -->
<!-- ============================================================================ -->

## Refinamiento Técnico (Developer)

<!-- ============================================================================ -->
<!-- SECCIÓN AGREGADA POR: Workflow refinamiento-tecnico                         -->
<!-- ETAPA: Refinamiento Técnico                                                  -->
<!-- RESPONSABLE: Developer                                                       -->
<!-- BASE: Refinamiento Directo (sin análisis arquitectónico previo)              -->
<!-- ============================================================================ -->

### Consideraciones Generales

**Basado en análisis arquitectónico:**
No — Refinamiento Directo sin análisis arquitectónico previo. Se realizó análisis técnico profundo del código base existente: 3 bottom-nav components, formulario `organization-config-page`, `AuthenticationService` con JWT decode, CSS Design Tokens en `styles.css`, modelos/gateways/adapters de `OrganizationConfig`, migraciones Flyway V1-V21, backend Clean Architecture (Spring WebFlux + R2DBC + MySQL).

**Nivel de complejidad:**
Alta — Historia con 13 criterios de aceptación que abarca 3 funcionalidades integradas (Branding Config + Theming Dinámico + Bottom Nav Unificado). Afecta 4 capas backend (model, usecase, entry-points, driven-adapters) + 4 capas frontend (domain, infrastructure, services, presentation). Requiere: migración de BD, nuevo servicio de extracción de colores (Canvas API + k-means), servicio de theming dinámico (CSS Variables override en `:root`), refactor de 3 componentes de navegación a 1 unificado con JSON config por rol. ~30 archivos entre nuevos y modificados. Componentes existentes reutilizados extensivamente: `OrganizationConfigPageComponent`, `OrganizationConfigGateway/Adapter`, `ToggleSwitchComponent`, CSS Design Tokens (`styles.css`).

**Riesgos técnicos conocidos:**
1. **Canvas API cross-origin:** La extracción de colores con `getImageData()` puede fallar si la imagen tiene restricciones CORS. Mitigación: la imagen se carga desde un FileReader local (no URL externa), eliminando el problema CORS.
2. **Tamaño de logo en Base64:** Enviar imagen como Base64 en JSON incrementa ~33% el tamaño. Mitigación: límite de 2MB configurado en `application.yaml` → máximo ~2.66MB en Base64, aceptable para un PUT de configuración.
3. **Rendimiento de k-means en imágenes grandes:** Algoritmo de cuantización puede ser lento con imágenes >4K. Mitigación: redimensionar a máximo 100x100 px en canvas antes de analizar (solo para extracción, no para almacenamiento).
4. **Eliminación de 3 bottom-nav:** Múltiples páginas importan estos componentes directamente (home, doorman pages). Mitigación: búsqueda exhaustiva de imports y reemplazo sistemático.
5. **Framework de testing frontend es Jasmine/Karma** (no Jest) — verificado en `package.json` y `.spec.ts` existentes. CODING_STANDARDS.md muestra ejemplos con Jest pero el proyecto usa Jasmine.

**Patrones y convenciones del equipo:**
- Backend: Clean Architecture Bancolombia, `@Getter @Builder(toBuilder=true) @AllArgsConstructor`, `Mono<T>/Flux<T>`, `StepVerifier`, `@ExtendWith(MockitoExtension.class)`, `@Component @RequiredArgsConstructor @Slf4j` para handlers, `RouterFunction<ServerResponse>` funcional, `ApiResponse.success()/error()`, migraciones Flyway `V{n}__description.sql`
- Frontend: Angular 18+ standalone, signals (`input()`, `output()`, `signal()`, `computed()`), `OnPush`, `@if/@for` control flow, `inject()` para DI, path aliases `@domain/*`/`@infrastructure/*`/`@presentation/*`/`@env/*`, `Result<T>` discriminated union con factories `success()`/`failure()`, `ApiEnvelope<T>` wrapper en adapters, BEM CSS (`.org-config__*`, `.admin-bottom-nav__item`), `jasmine.createSpyObj()` para mocks

**Dependencias nuevas a instalar:**
Ninguna — extracción de colores con Canvas API nativo (TypeScript puro), theming con CSS Variables nativo, sin librerías externas. Todo se implementa con Angular 18, RxJS, Spring WebFlux, R2DBC, Flyway, Jasmine/Karma existentes.

**Estrategia de testing:**
Backend: JUnit 5 + Mockito + StepVerifier + AssertJ | Tests unitarios para use cases (branding validation, color format), tests de integración con `@WebFluxTest` + `WebTestClient` para handler (upload logo, save branding) | Cobertura: ≥70% | Builders: Crear `OrganizationConfigurationBuilder` en `usecase/organization/builders/` — reutilizar `PorterBuilder` como referencia
Frontend: Jasmine 5.2 + Karma 6.4 | Tests unitarios para `ColorExtractionService`, `ThemingService`, `UnifiedBottomNavComponent` | Tests de componente para `OrganizationConfigPageComponent` (sección branding) | Cobertura: ≥70% | Mocks: `jasmine.createSpyObj()` para gateways

### Historias Relacionadas Consultadas

**Implementaciones similares analizadas:**
- HU #9 (Invitaciones): Extiende `OrganizationConfiguration` con `enableOwnerPermissionManagement` — patrón exacto de migración V16 + actualización de modelo/DTO/entity/adapter. Propone `owner-bottom-nav` que ahora se absorbe en el componente unificado.
- HU #2 (Onboarding Admin): `OnboardingFormComponent` — patrón de formulario con signal-based state, validaciones, y guardado. `OrganizationConfigPageComponent` ya implementado.
- HU #4 (Creación Portero): `PorterBuilder` — patrón Test Data Builder fluent para tests unitarios. Migraciones Flyway.
- HU #7 (Validación Portería): `DoormanBottomNavComponent` — componente de navegación específico por rol que será absorbido.
- HU #8 (Panel Administrativo): `AdminBottomNavComponent` — bottom-nav admin con indicador activo superior.

**Patrones de código reutilizados:**
1. Extensión de `OrganizationConfiguration` model+entity+DTO (de HU #9): agregar campos → migración → mapear en adapter
2. `OrganizationConfigPageComponent` formulario reactivo con signals (de HU #2): extender con sección de branding
3. `OrganizationConfigGateway`/`Adapter` patrón GET/PUT con `ApiEnvelope<T>` (existente): extender para incluir branding
4. CSS Variables override en `:root` (`document.documentElement.style.setProperty`) — patrón estándar web APIs
5. Test Data Builder fluent (de `PorterBuilder`): `aBuilder().withField(val).build()`
6. Bottom-nav BEM CSS (de `admin-bottom-nav`): `.unified-nav__*` siguiendo convención existente

**Mejores prácticas aplicadas:**
- Reutilizar CSS Design Tokens existentes en `styles.css` — el `ThemingService` solo overridea las CSS Variables, todos los componentes se adaptan automáticamente sin cambios individuales
- Extender modelo existente (`OrganizationConfiguration`) en lugar de crear tabla paralela — reduce duplicación
- Canvas API nativo para extracción de colores — cero dependencias nuevas
- JSON config para ítems de navegación — configurable por deploy sin cambios de BD
- Base64 en JSON para logo — consistente con API REST existente (PUT JSON), evita complejidad de multipart

---

## Tareas de Implementación (Developer)

### Fase 0: Infraestructura y Setup

#### Migraciones de Base de Datos
- [x] **Crear migración V22 — Agregar campos de branding a organization_configuration**
  - [x] Crear archivo de migración: `atlas/infrastructure/driven-adapters/r2dbc-postgresql/src/main/resources/db/migration/V22__add_branding_to_organization_configuration.sql`
  - [x] Agregar columnas: `logo_data MEDIUMBLOB NULL`, `logo_content_type VARCHAR(50) NULL`, `dominant_color VARCHAR(7) NULL`, `secondary_color VARCHAR(7) NULL`, `accent_color VARCHAR(7) NULL`
  - [ ] Ejecutar migración en ambiente de desarrollo
  - [ ] Verificar rollback funciona correctamente

#### Cambios de Configuración
- [x] **Agregar configuración de tamaño máximo de logo**
  - [x] Modificar `atlas/applications/app-service/src/main/resources/application.yaml`
  - [x] Agregar propiedad `atlas.branding.max-logo-size-bytes: 2097152` (2MB)
  - [x] Agregar propiedad `atlas.branding.allowed-content-types: image/png,image/jpeg`

---

### Fase 1: Backend — Modelo de Dominio y Gateway (AC: 1, 4)

#### 📦 Domain Model

- [x] **Extender modelo de dominio `OrganizationConfiguration`**
  - [x] Modificar `atlas/domain/model/src/main/java/co/com/atlas/model/organization/OrganizationConfiguration.java` — Agregar campos: `byte[] logoData`, `String logoContentType`, `String dominantColor`, `String secondaryColor`, `String accentColor`
  - [x] Agregar validación de formato hex en modelo: `isValidHexColor(String color)` método privado

- [x] **Extender gateway `OrganizationConfigurationRepository`**
  - [x] Modificar `atlas/domain/model/src/main/java/co/com/atlas/model/organization/gateways/OrganizationConfigurationRepository.java` — Sin cambios necesarios, `save()` ya es genérico

---

### Fase 2: Backend — Caso de Uso y Validaciones (AC: 1, 3, 4)

#### 📦 Domain Use Case

- [x] **Extender `OrganizationSettingsUseCase` con lógica de branding**
  - [x] Modificar `atlas/domain/usecase/src/main/java/co/com/atlas/usecase/organization/OrganizationSettingsUseCase.java`
  - [x] Agregar método `updateBranding(Long orgId, byte[] logoData, String contentType, String dominantColor, String secondaryColor, String accentColor)`
  - [x] Validar: formato PNG/JPG (`contentType` in `[image/png, image/jpeg]`), tamaño máximo (`logoData.length <= maxLogoSizeBytes`), colores en formato hex (`#RRGGBB`)
  - [x] Recibir `maxLogoSizeBytes` como parámetro del constructor (inyectado desde bean factory en `app-service/src/main/java/.../config/` con `@Value("${atlas.branding.max-logo-size-bytes}")`) — NO usar `@Value` en dominio
  - [x] Lanzar `BusinessException` con mensaje claro si validación falla

- [x] **Test unitario para validaciones de branding**
  - [x] Crear `atlas/domain/usecase/src/test/java/co/com/atlas/usecase/organization/OrganizationSettingsUseCaseTest.java`
  - [x] Test: formato de imagen inválido → `BusinessException`
  - [x] Test: tamaño excedido → `BusinessException`
  - [x] Test: color hex inválido → `BusinessException`
  - [x] Test: branding válido → `save()` ejecutado

- [x] **Crear Test Data Builder**
  - [x] Crear `atlas/domain/usecase/src/test/java/co/com/atlas/usecase/organization/builders/OrganizationConfigurationBuilder.java`
  - [x] Patrón fluent: `anOrganizationConfig().withDominantColor("#FF8C61").withLogoData(smallPng).build()`

---

### Fase 3: Backend — Adapter R2DBC (AC: 1)

#### 📦 Infrastructure Driven Adapters

- [x] **Extender entidad R2DBC con campos de branding**
  - [x] Modificar `atlas/infrastructure/driven-adapters/r2dbc-postgresql/src/main/java/co/com/atlas/r2dbc/organizationconfiguration/OrganizationConfigurationEntity.java`
  - [x] Agregar `@Column("logo_data") byte[] logoData`, `@Column("logo_content_type") String logoContentType`, `@Column("dominant_color") String dominantColor`, `@Column("secondary_color") String secondaryColor`, `@Column("accent_color") String accentColor`

- [x] **Actualizar mapper en adapter**
  - [x] Modificar `atlas/infrastructure/driven-adapters/r2dbc-postgresql/src/main/java/co/com/atlas/r2dbc/organizationconfiguration/OrganizationConfigurationRepositoryAdapter.java`
  - [x] Actualizar `toDomain()` y `toEntity()` con los nuevos campos de branding

---

### Fase 4: Backend — Entry Points API REST (AC: 1, 4)

#### 📦 Infrastructure Entry Points

- [x] **Extender DTOs de request/response**
  - [x] Modificar `OrganizationSettingsDto.java` — Agregar: `String logoBase64`, `String logoContentType`, `String dominantColor`, `String secondaryColor`, `String accentColor`
  - [x] Modificar `OrganizationSettingsResponse.java` — Agregar mismos campos (logo como Base64 string en response)

- [x] **Actualizar handler para procesar branding**
  - [x] Modificar `OrganizationSettingsHandler.java` — En método `updateSettings()`: decodificar `logoBase64` a `byte[]` con `Base64.getDecoder().decode()`, delegar a use case con validaciones
  - [x] En método `getSettings()`: encodear `logoData` a Base64 string para response

- [x] **Actualizar documentación OpenAPI en Router**
  - [x] Modificar `OrganizationSettingsRouterRest.java` — Actualizar `@Schema` en DTOs con nuevos campos de branding (`logoBase64`, `logoContentType`, `dominantColor`, `secondaryColor`, `accentColor`) y verificar que `@RouterOperations` refleje la estructura actualizada del request/response


---

### Fase 5: Frontend — Modelo de Dominio y Gateway (AC: 1, 6, 8)

#### 📦 Frontend Domain

- [ ] **Extender interfaz `OrganizationConfig`**
  - [ ] Modificar `atlas-platform/src/app/domain/models/organization/organization-config.model.ts`
  - [ ] Agregar campos opcionales: `logoBase64?: string`, `logoContentType?: string`, `dominantColor?: string`, `secondaryColor?: string`, `accentColor?: string`

- [ ] **Extender gateway abstract**
  - [ ] Modificar `atlas-platform/src/app/domain/gateways/organization/organization-config.gateway.ts` — sin cambios necesarios, `getConfig()` y `saveConfig()` ya son genéricos

- [ ] **Extender adapter HTTP**
  - [ ] Modificar `atlas-platform/src/app/infrastructure/adapters/organization/organization-config.adapter.ts` — sin cambios necesarios, ya envía/recibe `OrganizationConfig` completo

---

### Fase 6: Frontend — Servicio de Extracción de Colores (AC: 2, 3, 5)

#### 📦 Frontend Infrastructure Services

- [ ] **Crear `ColorExtractionService`** (AC: 2)
  - [ ] Crear `atlas-platform/src/app/infrastructure/services/color-extraction.service.ts`
  - [ ] Implementar método `extractColors(imageFile: File): Promise<{dominant: string, secondary: string, accent: string}>`
  - [ ] Usar Canvas API: crear `<canvas>` off-screen, dibujar imagen redimensionada (100x100 max), obtener pixel data con `getImageData()`
  - [ ] Implementar algoritmo k-means simplificado: agrupar pixels por clusters de color (k=5), seleccionar top 3 por frecuencia
  - [ ] Convertir RGB a hex (`#RRGGBB`)
  - [ ] Manejar error si imagen no se puede cargar → `Promise.reject` con mensaje claro

- [ ] **Test unitario para `ColorExtractionService`**
  - [ ] Crear `atlas-platform/src/app/infrastructure/services/color-extraction.service.spec.ts`
  - [ ] Test: imagen válida → retorna 3 colores hex
  - [ ] Test: resultado es hex válido (`/^#[0-9A-Fa-f]{6}$/`)
  - [ ] Mock de Canvas API con `jasmine.createSpyObj()`

---

### Fase 7: Frontend — Servicio de Theming Dinámico (AC: 6, 7, 8, 13)

#### 📦 Frontend Infrastructure Services

- [ ] **Crear `ThemingService`** (AC: 6, 7, 8)
  - [ ] Crear `atlas-platform/src/app/infrastructure/services/theming.service.ts`
  - [ ] Inyectar `OrganizationConfigGateway` para obtener branding
  - [ ] Método `applyTheme(config: OrganizationConfig): void` — aplica colores a CSS Variables en `:root`:
    - `--color-primary` → `dominantColor` (60%)
    - `--color-primary-light` → variación clara auto-generada (+30% luminosidad)
    - `--color-primary-dark` → variación oscura auto-generada (-20% luminosidad)
    - `--color-primary-rgb` → versión RGB del dominante para opacidades `rgba()`
    - `--gradient-primary` → gradiente con dominante y variación clara
    - Colores de acento y secundario como nuevas CSS variables: `--color-accent`, `--color-secondary`
  - [ ] Método `generateVariations(hexColor: string): {light: string, dark: string, rgb: string}` — genera opacidades/variaciones automáticamente
  - [ ] Método `loadAndApplyTheme(): void` — carga branding de organización, aplica theme, persiste en `localStorage` key `atlas_theme`
  - [ ] Método `applyDefaultTheme(): void` — aplica colores por defecto (`#FF8C61`) si no hay configuración
  - [ ] Método `clearTheme(): void` — limpia CSS Variables y localStorage (para logout)
  - [ ] Al iniciar: verificar `localStorage` para aplicar theme sin HTTP request (optimización)

- [ ] **Integrar `ThemingService` en flujo de login** (AC: 6)
  - [ ] Modificar `atlas-platform/src/app/services/authentication.service.ts` — después de `login()` exitoso, llamar `themingService.loadAndApplyTheme()`
  - [ ] Modificar `atlas-platform/src/app/app.component.ts` — en `ngOnInit()`, si hay sesión activa, aplicar theme desde `localStorage`

- [ ] **Test unitario para `ThemingService`**
  - [ ] Crear `atlas-platform/src/app/infrastructure/services/theming.service.spec.ts`
  - [ ] Test: `applyTheme()` con config válida → CSS Variables set en `document.documentElement`
  - [ ] Test: `applyDefaultTheme()` → `--color-primary` = `#FF8C61`
  - [ ] Test: `generateVariations()` → retorna light/dark/rgb válidos
  - [ ] Test: `clearTheme()` → CSS Variables removidas + localStorage limpio

---

### Fase 8: Frontend — Formulario de Branding (AC: 1, 2, 3, 4, 5)

#### 📦 Frontend Presentation

- [ ] **Extender `OrganizationConfigPageComponent` con sección de branding** (AC: 1, 5)
  - [ ] Modificar `atlas-platform/src/app/presentation/pages/admin/organization-config/organization-config-page.component.ts`
  - [ ] Agregar signals: `logoPreview = signal<string | null>(null)`, `extractedColors = signal<{dominant: string, secondary: string, accent: string} | null>(null)`, `isExtractingColors = signal(false)`
  - [ ] Agregar al formulario reactivo: `dominantColor`, `secondaryColor`, `accentColor` (FormControls)
  - [ ] Método `onLogoSelected(event: Event)`: leer archivo con FileReader, validar tipo/tamaño client-side, mostrar preview, invocar `ColorExtractionService.extractColors()`, autocompletar campos de color
  - [ ] Incluir validados colores en `saveConfig()`

- [ ] **Actualizar template HTML del formulario** (AC: 2, 3, 5)
  - [ ] Modificar `atlas-platform/src/app/presentation/pages/admin/organization-config/organization-config-page.component.html`
  - [ ] Agregar sección "Identidad Visual" después de secciones existentes:
    - Input file para logo (accept `image/png,image/jpeg`)
    - Previsualización de imagen (`<img [src]="logoPreview()">`  con `@if`)
    - Spinner de extracción de colores (`@if (isExtractingColors())`)
    - 3 inputs de color: dominante, secundario, acento — con `<input type="color">` + `<input type="text" pattern="#[0-9A-Fa-f]{6}">`
    - Mensajes de error para formato/tamaño inválidos

- [ ] **Actualizar estilos CSS del formulario** (AC: 5)
  - [ ] Modificar `atlas-platform/src/app/presentation/pages/admin/organization-config/organization-config-page.component.css`
  - [ ] Estilos BEM: `.org-config__branding-section`, `.org-config__logo-preview`, `.org-config__color-picker`
  - [ ] Preview de logo: max 200x200px, border-radius, shadow
  - [ ] Color pickers: layout horizontal, spacing, labels

- [ ] **Test de componente para sección branding** (AC: 2, 4)
  - [ ] Crear o extender `atlas-platform/src/app/presentation/pages/admin/organization-config/organization-config-page.component.spec.ts`
  - [ ] Test: seleccionar imagen válida → preview mostrado + colores extraídos
  - [ ] Test: seleccionar archivo no PNG/JPG → mensaje de error
  - [ ] Test: guardar con branding → `saveConfig()` incluye colores

---

### Fase 9: Frontend — Componente Unificado de Navegación (AC: 9, 10, 11, 12)

#### 📦 Frontend Presentation Organisms

- [ ] **Crear `UnifiedBottomNavComponent`** (AC: 9, 10, 11, 12)
  - [ ] Crear directorio `atlas-platform/src/app/presentation/ui/organisms/unified-bottom-nav/`
  - [ ] Crear `unified-bottom-nav.component.ts` — Standalone, `OnPush`, signals
  - [ ] Input `role: InputSignal<string>` — rol del usuario (`ADMIN_ATLAS`, `OWNER`, `RESIDENT`, `PORTERO_VIGILANTE`)
  - [ ] Output `fabAction = output<void>()` — emisión del FAB central
  - [ ] **JSON config** estático con ítems por rol:
    ```typescript
    const NAV_CONFIG: Record<string, NavItem[]> = {
      'OWNER': [{icon: 'bi-house', label: 'Inicio', route: '/home'}, {icon: 'bi-clock-history', label: 'Historial', route: '/owner/history'}, {type: 'fab'}, {icon: 'bi-megaphone', label: 'Difusión', route: '/announcements'}, {icon: 'bi-car-front', label: 'Vehículos', route: '/vehicles'}],
      'RESIDENT': [...mismos que OWNER...],
      'ADMIN_ATLAS': [{icon: 'bi-house', label: 'Inicio', route: '/home'}, {icon: 'bi-building', label: 'Unidades', route: '/admin/units'}, {icon: 'bi-person-badge', label: 'Porteros', route: '/admin/porters'}, {icon: 'bi-gear', label: 'Config', route: '/admin/organization-config'}, {icon: 'bi-three-dots', label: 'Más', route: '/admin/more'}],
      'PORTERO_VIGILANTE': [{icon: 'bi-qr-code-scan', label: 'Escanear', route: '/doorman/scan'}, {icon: 'bi-clock-history', label: 'Historial', route: '/doorman/history'}, {icon: 'bi-three-dots', label: 'Más', route: '/doorman/more'}]
    };
    ```
  - [ ] Computed signal `navItems` basado en `role()`
  - [ ] Computed signal `navStyle` — píldora para OWNER/RESIDENT, full-width para ADMIN/PORTERO

- [ ] **Crear template HTML** (AC: 9, 10)
  - [ ] Crear `unified-bottom-nav.component.html`
  - [ ] Estructura condicional: `@if (navStyle() === 'pill')` → estilo píldora con FAB | `@else` → estilo flat con indicador superior
  - [ ] FAB central: `<button class="unified-nav__fab" (click)="fabAction.emit()">` con ícono `bi-plus-lg`
  - [ ] Usar `routerLink` y `routerLinkActive` para ítems
  - [ ] Indicador activo: color de acento de theming (`var(--color-accent, var(--color-primary))`)

- [ ] **Crear estilos CSS** (AC: 9, 10, 11)
  - [ ] Crear `unified-bottom-nav.component.css`
  - [ ] Estilo píldora (OWNER/RESIDENT): border-radius 2.5rem, fondo `#EDEAE7`, sombra suave, 90% ancho, margen inferior 20px, FAB circular 60px con `var(--color-accent, var(--color-primary))`
  - [ ] Estilo flat (ADMIN): ancho completo, fondo `#E5E5E5`, sin border-radius, indicador superior 40px × 3px con `var(--color-accent, var(--color-primary))`, altura 70px
  - [ ] Estados activo/inactivo con CSS Variables de theming
  - [ ] Tipografía: 12px, font-weight 500 activo, 400 inactivo

- [ ] **Test de componente para `UnifiedBottomNavComponent`**
  - [ ] Crear `unified-bottom-nav.component.spec.ts`
  - [ ] Test: rol OWNER → muestra 5 ítems con FAB central
  - [ ] Test: rol ADMIN_ATLAS → muestra 5 ítems sin FAB, con indicador superior
  - [ ] Test: rol PORTERO_VIGILANTE → muestra ítems de portero
  - [ ] Test: click en FAB → emite `fabAction`
  - [ ] Test: click en ítem → navega a ruta correcta

---

### Fase 10: Frontend — Integración y Reemplazo de Bottom Navs (AC: 9, 10, 11, 13)

#### 📦 Frontend Presentation Pages

- [ ] **Reemplazar bottom-nav en `HomeComponent`** (AC: 9, 10)
  - [ ] Modificar `atlas-platform/src/app/presentation/pages/home/home.component.ts` — Reemplazar imports de `BottomNavComponent` y `AdminBottomNavComponent` por `UnifiedBottomNavComponent`
  - [ ] Modificar `atlas-platform/src/app/presentation/pages/home/home.component.html` — Reemplazar `@if (isAdmin())` con `<app-unified-bottom-nav [role]="userRole()">` usando computed signal del rol del usuario
  - [ ] Conectar output `(fabAction)` a método de generación QR existente

- [ ] **Reemplazar bottom-nav en páginas de portero** (AC: 11)
  - [ ] Buscar todas las páginas en `atlas-platform/src/app/presentation/pages/doorman/` que importen `DoormanBottomNavComponent`
  - [ ] Reemplazar por `UnifiedBottomNavComponent` con `role="PORTERO_VIGILANTE"`

- [ ] **Reemplazar bottom-nav en demás páginas admin/owner**
  - [ ] Buscar TODAS las páginas que importen cualquiera de los 3 bottom-nav antiguos
  - [ ] Reemplazar por `UnifiedBottomNavComponent` con el rol correspondiente

- [ ] **Eliminar componentes obsoletos**
  - [ ] Eliminar directorio `atlas-platform/src/app/presentation/ui/organisms/bottom-nav/`
  - [ ] Eliminar directorio `atlas-platform/src/app/presentation/ui/organisms/admin-bottom-nav/`
  - [ ] Eliminar directorio `atlas-platform/src/app/presentation/ui/organisms/doorman-bottom-nav/`

---

### Fase 11: Frontend — Adaptación de Componentes al Theming (AC: 13)

#### 📦 Frontend Presentation (Átomos, Moléculas, Organismos, Templates)

- [ ] **Auditar y adaptar componentes que usen colores hardcodeados** (AC: 13)
  - [ ] Buscar en todos los `.css` de `presentation/ui/` y `presentation/pages/` referencias a `#FF8C61`, `#FFB399`, `#FF6B3D` u otros colores hardcodeados
  - [ ] Reemplazar por CSS Variables correspondientes: `var(--color-primary)`, `var(--color-primary-light)`, `var(--color-primary-dark)`, `var(--color-accent)`, `var(--color-secondary)`
  - [ ] Verificar que `auth-template.component.css` use `var(--gradient-primary)` (ya lo hace)
  - [ ] Verificar que `home.component.css` reemplace colores hardcodeados como `#E3F2FD`, `#9C27B0` por variables derivadas del theming

- [ ] **Agregar CSS Variables de theming a `styles.css`** (AC: 7)
  - [ ] Modificar `atlas-platform/src/styles.css`
  - [ ] Agregar en `:root`: `--color-accent: var(--color-primary)`, `--color-secondary: var(--gray-600)` como defaults
  - [ ] Agregar variaciones: `--color-primary-bg: rgba(var(--color-primary-rgb), 0.08)`, `--color-primary-hover: rgba(var(--color-primary-rgb), 0.12)`, `--color-primary-border: rgba(var(--color-primary-rgb), 0.2)`

---

### Fase 12: QA y Deployment

#### 📦 Code Quality

- [ ] **Ejecutar Agente Peer Review**
- [ ] **Resolver incidentes del Peer Review** (condicional)

#### 📦 Deployment DEV

- [ ] **Crear Pull Request**
- [ ] **Ejecutar pipeline deployment DEV**

#### 📦 Testing Manual

- [ ] **Diseñar set de pruebas manuales**
- [ ] **Ejecutar pruebas manuales**

---

**Notas sobre vinculación con Criterios de Aceptación:**
- Cada tarea indica qué criterio(s) de aceptación ayuda a completar usando (AC: X, Y, Z)
- Las tareas de infraestructura (Fase 0) no tienen vinculación directa a AC específicos
- Las Fases 1-4 cubren backend (AC: 1, 3, 4)
- Las Fases 5-8 cubren frontend - modelo, servicios y formulario (AC: 1-8)
- Las Fases 9-10 cubren navegación unificada (AC: 9, 10, 11, 12)
- La Fase 11 cubre adaptación global de theming (AC: 13)

## Definición de Terminado (Inicial)

- [ ] Funcionalidad implementada según criterios de aceptación
- [ ] Formulario de configuración con carga de logo, previsualización y campos de colores autocompletados
- [ ] Extracción de colores dominantes funcionando en cliente (TypeScript)
- [ ] Validación estricta de formatos PNG/JPG y tamaño máximo configurable
- [ ] Persistencia de imagen (bytes) y colores en base de datos
- [ ] Servicio de theming que aplica colores de organización al login
- [ ] Generación automática de variaciones/opacidades de colores
- [ ] Colores por defecto funcionando cuando no hay configuración
- [ ] Componente unificado de bottom navigation reemplazando los 3 existentes
- [ ] Configuración de ítems de navegación por JSON en TypeScript por rol
- [ ] FAB "+" vinculado a generación de autorizaciones QR (HU #6) para no-admin
- [ ] Todos los componentes del frontend adaptados al theming dinámico
- [ ] Validaciones funcionando correctamente
- [ ] Mensajes de error y éxito implementados

---

## Registro de Cambios

| Fecha | Versión | Descripción | Autor |
|-------|---------|-------------|-------|
| 2026-02-22 | 1.0 | Historia creada por PO | yanuard.montialgre (PO) |
| 2026-02-25 | 1.1 | Refinamiento técnico: 12 fases (Fase 0 infraestructura + 10 implementación + Fase 12 QA), migración V22 branding, ColorExtractionService Canvas API, ThemingService CSS Variables override, UnifiedBottomNavComponent JSON config por rol, extensión OrganizationConfiguration model/entity/DTO, checklist validado | yanuard.montialgre (Developer) |

---

## Metadata

**Autor:** yanuard.montialgre (PO)  
**Fecha:** 2026-02-22  
**Historias relacionadas:** HU #1, HU #2, HU #6, HU #8, HU #9

---

## Métricas de redacción de la historia de usuario con el PO 

**Tiempo con Método Ceiba:** 6 minutos  
**Tiempo estimado tradicional:** 72 minutos  
**Optimización:** 92%

---
